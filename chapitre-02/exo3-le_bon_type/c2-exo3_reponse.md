 pour chacune des valeurs voici le type de base le plus adaper et justifiez par la taille et la plage:

 ```
* un âge = type: char, taille=1 octet  plage 0 a 255;
*le nombre d'habitants de la Terre = long long, taille= 8 octets.
*une température en degrés = float, taille= 4 octets,plage = elle contient les decimals.
* le caractère tapé au clavier = char , taille= 1 octet;
*le fait qu'une porte soit ouverte = char
* le nombre de pixels d'une image de 4000 × 3000 = int , taille = 4 octets, plage =  −2 147 483 648 a 2 147 483 648
*un solde bancaire en francs CFA = double , taille 8 octets 
```
 justification: opération que le type refuse.

 ```
$ un âge = refuse les valeur negative;
$ le nombre d'habitants de la Terre  =  le stcakage d'un plus grand nombre que celui de la plage autorisser
$ une température en degrés = l'utilisation de l'operateur modulo
$ le caractère tapé au clavier = les cahine de caracteres 
$ le fait qu'une porte soit ouverte  = 
$  le nombre de pixels d'une image de 4000 × 3000  = un nombre au dela de la plage autoriser
$ un solde bancaire en francs CFA = 
```

