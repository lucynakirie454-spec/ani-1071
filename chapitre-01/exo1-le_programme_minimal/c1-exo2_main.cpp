#include <cstdio>
int main()
{
printf("MENGUE KERINE");
printf("yaounde");

//lorsqu'on enleve aller a la ligne les deux phrases ne sont plus affiches sur deux lignes differentes.ils apparaissent sur une meme ligne,l'un a la suite de l'autre.
//cela ne fait pas un bug car on n'a pas demander a la machine d'aller a la ligne.donc l'ordinateur execute juste ce qui lui a ete demander.
        return 0;
}